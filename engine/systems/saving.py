# todo saving :) It's ECS so it should be fairly easy to serialize the entities and their components.
# Most likely need an asset manager first so we aren't having Surface's directly in a component's data as it cant
# serialize.